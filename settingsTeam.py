from team import Team
from settings import Settings

class SettingsTeam(object):
    """docstring for SettingsTeam"""
    def __init__(self, arg):
        super(SettingsTeam, self).__init__()
        self.arg = arg

self._PK
self._SETTINGS = Settings()
self._TEAM = Team()
